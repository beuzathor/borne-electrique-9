```astro
---
interface Props {
  name: string;
  value: string;
  label: string;
}

const { name, value, label } = Astro.props;
---
<label class="flex items-center">
  <input
    type="checkbox"
    {name}
    {value}
    class="rounded border-gray-300 text-indigo-600 focus:ring-indigo-500"
  />
  <span class="ml-2 text-sm text-gray-600 capitalize">{label}</span>
</label>
```